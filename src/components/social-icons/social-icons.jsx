import { Icon, IconButton, TagLeftIcon } from "@chakra-ui/react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const SocialIcons = ({ icon }) => {
    return (
        <p></p>
    );
}

export default SocialIcons;